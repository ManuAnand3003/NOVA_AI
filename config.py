# config.py

# Default input mode for the AI assistant ('voice' or 'text')
DEFAULT_INPUT_MODE = 'text'

# GPT Model to use (e.g., "gpt-3.5-turbo", "gpt-4")
GPT_MODEL = "gpt-3.5-turbo"

# Add other configurations here as needed
# For example, paths to models, thresholds for sentiment, etc.